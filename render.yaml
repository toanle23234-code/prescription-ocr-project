services:
  - type: web
    name: prescription-ocr-backend
    runtime: docker
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DB_BACKEND
        value: sqlite
      - key: FLASK_ENV
        value: production
      - key: ALLOW_DEV_OTP_FALLBACK
        value: true
